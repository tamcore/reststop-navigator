apiVersion: v1
kind: Service
metadata:
  name: {{ include "reststop-navigator.fullname" . }}
  labels:
    {{- include "reststop-navigator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "reststop-navigator.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
